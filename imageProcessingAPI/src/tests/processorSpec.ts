import processor from "../utilities/processor";

describe("Image processor", () => {
  it("should accept .jpg and .png image types", () => {
    expect(0).toBe(0);
  });

  it("should reject nonsensical query parameters", () => {});
});
